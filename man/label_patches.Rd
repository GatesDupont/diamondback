% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label.R
\name{label_patches}
\alias{label_patches}
\title{Label contiguous patches in a categorical or binary raster}
\usage{
label_patches(
  x,
  class = NULL,
  directions = 8,
  mask = NULL,
  na = c("outside", "background"),
  crop = NULL,
  output = NULL,
  overwrite = FALSE,
  max_memory_frac = 0.6,
  memory_limit = NULL,
  validate = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{x}{A \code{SpatRaster}, a raster filename, or a matrix.}

\item{class}{Which values are foreground. \code{NULL} (default) treats \code{x} as
binary: any non-zero, non-\code{NA} value is foreground. A numeric scalar selects
one class from a categorical raster. A numeric vector labels each of those
classes separately. \code{"all"} discovers every distinct value and labels each
separately. With more than one class, patch IDs are unique across classes
and \code{metrics$class} records which class each patch belongs to.}

\item{directions}{Connectivity: \code{8} (default, queen --- diagonal neighbours
are connected) or \code{4} (rook).}

\item{mask}{Optional study-area raster, aligned with \code{x}. Cells that are zero
or \code{NA} in the mask are outside the analysis domain. Must match \code{x}'s
geometry exactly; diamondback will not resample silently.}

\item{na}{How to treat \code{NA} cells inside the mask. \code{"outside"} (default)
excludes them from the analysis domain and treats their boundary with a
patch as domain edge. \code{"background"} treats them as valid background, which
makes them habitat edge.}

\item{crop}{Crop to the mask's bounding box before labelling, which can cut
memory use substantially when the study area is a small part of the raster.
Defaults to \code{TRUE} when \code{mask} is supplied.}

\item{output}{Optional path to write the labelled raster to. When given, the
raster is written and the result stores the path rather than holding cells
in memory.}

\item{overwrite}{Overwrite \code{output} if it exists.}

\item{max_memory_frac}{Largest fraction of detected available RAM the array
may occupy. The operation errors before allocating if the estimate exceeds
this.}

\item{memory_limit}{Explicit memory ceiling in bytes, overriding detection.}

\item{validate}{Run \code{\link[=validate_patch_result]{validate_patch_result()}} afterwards. Costs a second pass;
worth it while setting a workflow up.}

\item{quiet}{Suppress progress reporting.}
}
\value{
A \link{patch_result} object. \verb{$patches} gives the labelled \code{SpatRaster},
\verb{$metrics} a data frame with one row per patch (ID, class and cell count
only --- use \code{\link[=patch_metrics]{patch_metrics()}} for geometry), and \verb{$metadata} everything
needed to reproduce the run.
}
\description{
Connected-component labelling via \code{scipy.ndimage.label()}, with explicit
handling of missing values and study-area masks. This is the workhorse of the
package: on rasters of hundreds of millions of cells it is typically far
faster than \code{\link[terra:patches]{terra::patches()}}, and it produces the same partition.
}
\section{Cell states}{

diamondback keeps four cell states apart, and never lets \code{NA} silently become
foreground or background:
\itemize{
\item \strong{patch} --- a foreground cell, labelled \verb{1..N};
\item \strong{background} --- a valid, in-domain cell that is not the target class, \code{0};
\item \strong{outside the domain} --- excluded by \code{mask}, \code{NA} in the returned raster;
\item \strong{missing} --- \code{NA} in the source but inside the mask, also \code{NA} in the
returned raster.
}

The returned raster cannot distinguish the last two, because a raster has one
no-data value. Exact counts for all four are kept in \code{result$metadata$cells},
and \code{\link[=patch_domain]{patch_domain()}} reconstructs the distinction as a categorical raster.

\code{NA} is never foreground. It is not background either unless you say so with
\code{na = "background"}, which is the right choice when a raster uses \code{NA} to mean
genuine absence rather than lack of knowledge.
}

\examples{
# The two blocks touch only at a corner.
m <- matrix(c(1, 1, 0, 0,
              1, 1, 0, 0,
              0, 0, 1, 1,
              0, 0, 1, 1), nrow = 4, byrow = TRUE)

# Diagonal contact joins them under 8-connectivity, but not under 4.
label_patches(m, directions = 8, quiet = TRUE)$metadata$n_patches  # 1
label_patches(m, directions = 4, quiet = TRUE)$metadata$n_patches  # 2
}
\seealso{
\code{\link[=patch_metrics]{patch_metrics()}} for geometry, \code{\link[=analyze_patches]{analyze_patches()}} to do both,
\code{\link[=compare_patches]{compare_patches()}} for change over time.
}
