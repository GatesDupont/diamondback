% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_patches}
\alias{compare_patches}
\title{Compare patch configurations at two times}
\usage{
compare_patches(
  x1,
  x2,
  class = NULL,
  directions = 8,
  mask = NULL,
  na = c("outside", "background"),
  min_overlap_cells = 1L,
  min_overlap_prop = 0,
  check_domain = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{x1, x2}{The two times. Each may be a \link{patch_result}, or any input
\code{\link[=label_patches]{label_patches()}} accepts, in which case it is labelled first using
\code{class}, \code{directions}, \code{mask} and \code{na}.}

\item{class, directions, mask, na}{Passed to \code{\link[=label_patches]{label_patches()}} when \code{x1}/\code{x2}
need labelling. Ignored for inputs that are already a \link{patch_result}.}

\item{min_overlap_cells}{Minimum overlapping cells for a link to count.}

\item{min_overlap_prop}{Minimum overlap as a proportion of \strong{either} patch
for a link to count. A link survives if it is a meaningful share of one
side, so a small patch absorbed into a large one is not discarded.}

\item{check_domain}{Warn when the two analysis domains differ, which makes
appearance and disappearance ambiguous.}

\item{quiet}{Suppress progress reporting.}
}
\value{
A \code{patch_comparison} object with \verb{$overlaps}, \verb{$events}, \verb{$lineages}
and \verb{$metadata}.

\verb{$overlaps} columns: \code{id1}, \code{id2}, \code{cells1}, \code{cells2}, \code{overlap_cells},
\code{overlap_area_m2}, \code{prop_t1_retained} (share of the t1 patch now in this t2
patch), \code{prop_t2_inherited} (share of the t2 patch that came from this t1
patch), \code{rank_from_t1} (this t2 patch's rank among the t1 patch's
descendants), \code{rank_from_t2} (this t1 patch's rank among the t2 patch's
predecessors), \code{is_primary_predecessor}, \code{is_primary_descendant}.

\verb{$events} columns: \code{time}, \code{patch_id}, \code{cells}, \code{event}, \code{n_links} (number
of descendants for a t1 patch, of predecessors for a t2 patch),
\code{primary_id} (mother patch for t2 rows, main descendant for t1 rows),
\code{primary_overlap_cells}, \code{prop_primary} (share of this patch shared with its
primary link), \code{primary_tie}, \code{lineage_id}.
}
\description{
Cross-tabulates the cells shared by patches at time 1 and time 2, identifies
each patch's primary predecessor and descendant, and classifies what happened
to it: persistence, split, merger, complex reconfiguration, appearance or
disappearance.
}
\section{What is returned, and why three tables}{

\itemize{
\item \verb{$overlaps} --- one row per overlapping pair. This is the raw, complete
evidence, including both directional proportions and both ranks. \strong{No
lineage rule is baked in here}, so you can re-derive lineage under any rule
you prefer without re-running the expensive step.
\item \verb{$events} --- one row per patch (both times), with its event class, its
primary predecessor or descendant, and how much of it was retained.
\item \verb{$lineages} --- one row per connected component of the bipartite overlap
graph. A component is the honest unit for a merger: two patches becoming one
is a fact about the group, not about either patch.
}
}

\section{Event classification}{

Classes are assigned from the structure of each component, counting only links
that pass the thresholds:\tabular{ll}{
   class \tab meaning \cr
   \code{persistence} \tab one patch at t1, one at t2 \cr
   \code{split} \tab one patch at t1, several at t2 \cr
   \code{merger} \tab several at t1, one at t2 \cr
   \code{complex} \tab several at both --- a split and merger together \cr
   \code{disappearance} \tab at t1 with no qualifying overlap at t2 \cr
   \code{appearance} \tab at t2 with no qualifying overlap at t1 \cr
}


\code{persistence} says the patches are linked, not that nothing changed: a patch
that lost 90\% of its area but stayed connected is still \code{persistence}. Use
\code{prop_primary} and the area columns for magnitude.
}

\section{Choosing the mother patch}{

The primary predecessor of a time-2 patch is the time-1 patch contributing the
most overlapping cells. Ties break deterministically --- larger time-1 patch
first, then lower ID --- and any broken tie is flagged in \code{primary_tie} so it
is auditable rather than silent. Primary descendants are chosen symmetrically.

This is a default. \verb{$overlaps} has \code{prop_t1_retained} and \code{prop_t2_inherited}
as well as the counts, so a rule based on proportion rather than absolute
overlap is a one-line re-derivation.
}

\section{Thresholds, and seeing what they did}{

Defaults keep every overlap, because discarding evidence should be a decision
you make rather than one made for you. But classification is acutely sensitive
to slivers: a patch that donates 99.9\% of itself to one descendant and a
single cell to another is called a \code{split} under \code{min_overlap_cells = 1},
which is arguably not a meaningful description of what happened.

So nothing is hidden. Thresholded links are \strong{flagged, not deleted}:
\verb{$overlaps} keeps every overlapping pair with a \code{passes_threshold} column.
And every patch is classified \strong{twice} --- \code{event} uses only links that
passed, \code{event_all_overlaps} uses all of them, and
\code{threshold_changed_event} marks the patches where the two disagree.
\code{metadata$n_events_changed_by_threshold} counts them, and both thresholds are
stored in \code{metadata}.

The point is that two analyses of the same rasters cannot disagree without the
cause being visible in the output. If \code{threshold_changed_event} is all
\code{FALSE}, your thresholds did not matter and the classification is robust. If
it is not, those are exactly the patches to look at.

\strong{Which knob suppresses a sliver.} A link passes \code{min_overlap_prop} if it is
a big enough share of \strong{either} side, so that a small patch absorbed whole
into a large one is not discarded. The consequence is worth knowing: a
one-cell descendant is 100\% of \emph{itself}, so \code{min_overlap_prop} can never
suppress it, however large you set it. Use \code{min_overlap_cells} for absolute
slivers, and \code{min_overlap_prop} for links that are trivial to both sides.
}

\examples{
\dontshow{if (diamondback_ready()) withAutoprint(\{ # examplesIf}
# A patch splits in two between t1 and t2.
m1 <- matrix(0, 5, 5); m1[2:4, 2:4] <- 1
m2 <- matrix(0, 5, 5); m2[2, 2:4] <- 1; m2[4, 2:4] <- 1
cmp <- compare_patches(m1, m2, quiet = TRUE)
cmp$events[, c("time", "patch_id", "event", "primary_id")]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=track_patch_series]{track_patch_series()}} for more than two times.
}
