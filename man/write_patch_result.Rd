% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{write_patch_result}
\alias{write_patch_result}
\title{Write a patch result to disk}
\usage{
write_patch_result(x, dir, overwrite = FALSE, quiet = FALSE)
}
\arguments{
\item{x}{A \link{patch_result}.}

\item{dir}{Directory to write to; created if needed.}

\item{overwrite}{Overwrite existing files in \code{dir}.}

\item{quiet}{Suppress progress reporting.}
}
\value{
\code{dir}, invisibly.
}
\description{
Saves a \link{patch_result} as a small, durable directory: the labelled raster as
GeoTIFF, the metric table as CSV, and the metadata as JSON. Nothing is
pickled and nothing depends on an R or Python session, so results stay
readable years later and from other tools.
}
\details{
This is also the cache format used by \code{\link[=analyze_patches]{analyze_patches()}}, so a result you
saved and a result it cached are interchangeable.
}
\examples{
m <- matrix(c(1, 1, 0, 0, 1, 0, 0, 1, 1), nrow = 3)
res <- analyze_patches(m, quiet = TRUE)
d <- file.path(tempdir(), "patches_demo")
write_patch_result(res, d, overwrite = TRUE, quiet = TRUE)
read_patch_result(d)
}
\seealso{
\code{\link[=read_patch_result]{read_patch_result()}}
}
