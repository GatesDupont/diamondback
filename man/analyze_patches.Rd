% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{analyze_patches}
\alias{analyze_patches}
\title{Label patches and calculate their metrics}
\usage{
analyze_patches(
  x,
  class = NULL,
  directions = 8,
  mask = NULL,
  na = c("outside", "background"),
  crop = NULL,
  output_dir = NULL,
  cache = TRUE,
  metrics = TRUE,
  units = c("m", "km", "cells"),
  edge_depth = NULL,
  fingerprint = c("auto", "full", "fast"),
  overwrite = FALSE,
  max_memory_frac = 0.9,
  memory_limit = NULL,
  validate = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{x}{A \code{SpatRaster}, a raster filename, or a matrix.}

\item{class}{Which values are foreground. \strong{One element of \code{class} is one
class}:
\itemize{
\item \code{NULL} (default) --- treat \code{x} as binary: any non-zero, non-\code{NA} value is
foreground.
\item a numeric vector --- \strong{one} class made of those values, e.g.
\code{class = c(41, 42, 43)} labels deciduous, evergreen and mixed forest as a
single forest class, connected across the boundaries between them.
\item a list --- several classes, labelled separately and never joined to each
other, e.g. \code{class = list(forest = c(41, 42, 43), wetland = c(90, 95))}.
Names become the \code{class} column in \verb{$metrics}; unnamed groups are labelled
by their values (\code{"41+42+43"}).
\item \code{"all"} --- every distinct value becomes its own class.
}

A value may belong to only one class. With more than one class, patch IDs
are unique across classes and \code{metrics$class} records which class each patch
belongs to.}

\item{directions}{Connectivity: \code{8} (default, queen --- diagonal neighbours
are connected) or \code{4} (rook).}

\item{mask}{Optional study-area raster, aligned with \code{x}. Cells that are zero
or \code{NA} in the mask are outside the analysis domain. Must match \code{x}'s
geometry exactly; diamondback will not resample silently.}

\item{na}{How to treat \code{NA} cells inside the mask. \code{"outside"} (default)
excludes them from the analysis domain and treats their boundary with a
patch as domain edge. \code{"background"} treats them as valid background, which
makes them habitat edge.}

\item{crop}{Crop to the mask's bounding box before labelling, which can cut
memory use substantially when the study area is a small part of the raster.
Defaults to \code{TRUE} when \code{mask} is supplied.}

\item{output_dir}{Directory for the result. When set, the labelled raster,
metrics and metadata are written there, and a matching cached result is
reused. When \code{NULL} (default) nothing is written and nothing is cached.}

\item{cache}{Cache behaviour: \code{TRUE} (default) uses a matching cached result,
\code{FALSE} recomputes and overwrites, \code{"read"} uses the cache but does not
write.}

\item{metrics}{Compute \code{\link[=patch_metrics]{patch_metrics()}}. \code{FALSE} gives labels and cell
counts only.}

\item{units, edge_depth}{Passed to \code{\link[=patch_metrics]{patch_metrics()}}.}

\item{fingerprint}{How thoroughly the source is identified for caching and
provenance. \code{"auto"} (default) hashes file contents under 200 MB and falls
back to size and modification time above that. \code{"full"} always hashes the
contents, however large: slower, but immune to a file being rewritten at the
same size with its timestamp restored. \code{"fast"} never hashes. The mode is
recorded and forms part of the cache key, so a result cached under a weaker
fingerprint is never reused for a request that asked for a stronger one.}

\item{overwrite}{Overwrite \code{output} if it exists.}

\item{max_memory_frac}{Largest fraction of this machine's \strong{physical} RAM the
arrays may occupy. Above it the operation stops before allocating, because
the job cannot fit however the machine is used. Below it, diamondback will
run and merely warn if memory is currently tight -- an operating system
pages and reclaims, so a job larger than the free memory of the moment is
usually slow rather than impossible, and refusing it would be a false alarm.}

\item{memory_limit}{Explicit ceiling in bytes, used instead of the detected
physical RAM. For the rare case where you know the budget better than the
machine does, such as a container with a cgroup limit.}

\item{validate}{Run \code{\link[=validate_patch_result]{validate_patch_result()}} afterwards. Costs a second pass;
worth it while setting a workflow up.}

\item{quiet}{Suppress progress reporting.}
}
\value{
A \link{patch_result}.
}
\description{
The function most workflows should call. Combines \code{\link[=label_patches]{label_patches()}} and
\code{\link[=patch_metrics]{patch_metrics()}}, and adds caching: with \code{output_dir}, the result is written
there, and a later call with the same inputs reads it back instead of
relabelling.
}
\section{Caching}{

A cached result is reused only when it is provably the same computation. The
key covers the source fingerprint, the raster geometry, the class, the mask,
the connectivity, the NA policy, and the labelling algorithm version. Change
any of them and the work is redone, with a message naming the field that
differed.

Things that deliberately do \strong{not} invalidate a cache: metric options, units,
progress settings, and the package version. None of them can change the
labels.

A large in-memory raster with no file behind it cannot be fingerprinted, and
is therefore never served from cache. Point \code{x} at a file to get caching.
}

\section{How strong is the fingerprint?}{

Under the default \code{fingerprint = "auto"}, files under 200 MB are identified by
a content hash, and larger ones by path, size and modification time. Size and
mtime are a heuristic, not proof: a file rewritten at the same size with its
timestamp restored (\code{touch -r}, some sync tools, a restored backup) would be
treated as unchanged and the stale result reused.

If that matters — and on a pipeline whose inputs are regenerated it might —
use \code{fingerprint = "full"} to hash contents regardless of size. It costs a
read of the file, which is still far cheaper than relabelling. The mode is
part of the cache key, so switching to \code{"full"} correctly invalidates anything
cached under \code{"auto"} rather than trusting it.
}

\examples{
\dontshow{if (diamondback_ready()) withAutoprint(\{ # examplesIf}
m <- matrix(c(1, 1, 0, 0,
              1, 1, 0, 1,
              0, 0, 0, 1,
              0, 1, 1, 1), nrow = 4, byrow = TRUE)
res <- analyze_patches(m, quiet = TRUE)
res$metrics[, c("patch_id", "cells", "perimeter", "touches_domain_edge")]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=label_patches]{label_patches()}}, \code{\link[=patch_metrics]{patch_metrics()}}, \code{\link[=compare_patches]{compare_patches()}}
}
