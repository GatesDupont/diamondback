% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{patch_result}
\alias{patch_result}
\title{Patch analysis results}
\description{
The object returned by \code{\link[=label_patches]{label_patches()}} and \code{\link[=analyze_patches]{analyze_patches()}}. It is a
plain list underneath, so nothing is hidden, with three components you will
use directly:
}
\details{
\itemize{
\item \verb{$patches} --- the labelled \code{SpatRaster}. This is an accessor, not stored
data: if the run wrote to disk it opens the file; if it stayed in memory it
returns the raster it holds. Either way you get a \code{SpatRaster}.
\item \verb{$metrics} --- one row per patch. After \code{\link[=label_patches]{label_patches()}} this has patch ID
and cell count; after \code{\link[=patch_metrics]{patch_metrics()}} or \code{\link[=analyze_patches]{analyze_patches()}} it has the
full geometry.
\item \verb{$metadata} --- everything needed to reproduce the run: source fingerprint,
geometry, class, connectivity, NA policy, cell-state counts, and backend
versions.
}

\verb{$path} gives the file the labels were written to, or \code{NULL}.
}
\section{Arrays}{

A result also carries a handle to the NumPy arrays from the run, which is
what lets \code{\link[=patch_metrics]{patch_metrics()}} and \code{\link[=compare_patches]{compare_patches()}} avoid relabelling. These
do not survive \code{\link[=saveRDS]{saveRDS()}} or a session restart; the arrays are rebuilt from
the labelled raster when needed, so a restored result still works.
}

\seealso{
\code{\link[=label_patches]{label_patches()}}, \code{\link[=patch_metrics]{patch_metrics()}}, \code{\link[=write_patch_result]{write_patch_result()}}
}
