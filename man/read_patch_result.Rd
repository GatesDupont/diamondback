% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_patch_result}
\alias{read_patch_result}
\title{Read a patch result from disk}
\usage{
read_patch_result(dir)
}
\arguments{
\item{dir}{Directory written by \code{\link[=write_patch_result]{write_patch_result()}}.}
}
\value{
A \link{patch_result}.
}
\description{
Reads back what \code{\link[=write_patch_result]{write_patch_result()}} wrote. The labelled raster is opened
from the file rather than loaded, so reading a result is cheap regardless of
its size.
}
\details{
The NumPy arrays from the original run are gone, so the first call to
\code{\link[=patch_metrics]{patch_metrics()}} or \code{\link[=compare_patches]{compare_patches()}} on a restored result rebuilds them
from the raster. Everything works; the first such call just pays a read.
}
\examples{
m <- matrix(c(1, 1, 0, 0, 1, 0, 0, 1, 1), nrow = 3)
d <- file.path(tempdir(), "patches_demo2")
write_patch_result(analyze_patches(m, quiet = TRUE), d, overwrite = TRUE, quiet = TRUE)
read_patch_result(d)
}
\seealso{
\code{\link[=write_patch_result]{write_patch_result()}}
}
