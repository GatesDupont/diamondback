% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_patch_result}
\alias{validate_patch_result}
\title{Validate a patch result}
\usage{
validate_patch_result(x, error = FALSE, quiet = FALSE)
}
\arguments{
\item{x}{A \link{patch_result}.}

\item{error}{Raise an error on failure instead of returning the report.}

\item{quiet}{Suppress the report.}
}
\value{
A list with \code{ok} (logical) and \code{checks} (a data frame with one row per
check: \code{check}, \code{passed}, \code{detail}), invisibly.
}
\description{
Re-derives facts about a result and checks them against what the result
claims. These are independent checks, not assertions restating the code that
produced the numbers: patch counts are recounted from the raster, foreground
cells are recounted from the cell states, and the sums are compared.
}
\details{
Run this while setting a workflow up, and on the first real run of a long
pipeline. \code{label_patches(validate = TRUE)} wires it in automatically.
}
\section{What is checked}{

\itemize{
\item every patch ID from \verb{1..N} is present and consecutive;
\item the foreground cell count equals the sum of patch sizes;
\item the number of patches in \verb{$metrics} equals the number in the raster;
\item no foreground cell lies outside the mask;
\item no patch has zero cells;
\item patch IDs fit the integer type used;
\item \code{edge_valid + edge_domain} equals \code{perimeter}, and edge counts are
consistent with edge lengths;
\item core area never exceeds total area.
}
}

\examples{
\dontshow{if (diamondback_ready()) withAutoprint(\{ # examplesIf}
m <- matrix(c(1, 1, 0, NA, 1, 0, 0, 1, 1), nrow = 3)
v <- validate_patch_result(analyze_patches(m, quiet = TRUE))
v$ok
\dontshow{\}) # examplesIf}
}
