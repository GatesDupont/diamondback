% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{patch_domain}
\alias{patch_domain}
\title{Reconstruct the four-state cell domain as a raster}
\usage{
patch_domain(x, filename = NULL, overwrite = FALSE)
}
\arguments{
\item{x}{A \link{patch_result}.}

\item{filename}{Optional path to write to.}

\item{overwrite}{Overwrite \code{filename} if it exists.}
}
\value{
A categorical \code{SpatRaster}.
}
\description{
The labelled raster collapses "outside the domain" and "missing" into \code{NA},
because a raster has only one no-data value. This rebuilds the full
distinction as a categorical raster with levels \code{outside}, \code{missing},
\code{background} and \code{patch}.
}
\details{
The source raster is re-read, so this costs a pass over the data. The exact
counts are always available without that, in \code{result$metadata$cells}.
}
\examples{
m <- matrix(c(1, 1, NA, 0, 0, 1, 0, 1, 1), nrow = 3)
res <- label_patches(m, quiet = TRUE)
terra::freq(patch_domain(res))
}
