% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{patch_core_area}
\alias{patch_core_area}
\title{Core area: habitat beyond a given distance from an edge}
\usage{
patch_core_area(
  x,
  depth,
  units = c("m", "km", "cells"),
  edge = c("all", "background"),
  core_raster = FALSE,
  max_memory_frac = 0.6,
  memory_limit = NULL,
  quiet = FALSE
)
}
\arguments{
\item{x}{A \link{patch_result}, or a labelled \code{SpatRaster}.}

\item{depth}{Edge depth. In map units unless \code{units = "cells"}.}

\item{units}{Units of \code{depth} and of the returned core area columns: \code{"m"}
(default), \code{"km"}, or \code{"cells"}.}

\item{edge}{What counts as an edge source: \code{"all"} or \code{"background"}.}

\item{core_raster}{Also return a raster of core cells, as
\code{result$core_raster}, labelled with patch IDs and \code{0} elsewhere.}

\item{max_memory_frac, memory_limit}{Memory guards. The distance transform
holds a float64 array, making this the most memory-hungry step in the
package (roughly 13 bytes per cell).}

\item{quiet}{Suppress progress reporting.}
}
\value{
The \link{patch_result} with \code{core_cells}, \code{core_area_m2}, \code{core_area_ha},
\code{core_area_km2} (or \code{core_area_cells}) and \code{core_fraction} added to
\verb{$metrics}. \code{core_fraction} is core cells over total cells, so a patch with
no interior gets \code{0}.
}
\description{
Calculates, for each patch, the cells lying further than \code{depth} from the
patch edge, using an exact Euclidean distance transform
(\code{scipy.ndimage.distance_transform_edt}) rather than repeated erosion. One
pass, any depth, and non-square cells handled exactly via anisotropic
sampling.
}
\section{How distance is measured}{

The transform gives, for each habitat cell, the Euclidean distance from its
\strong{centre} to the \strong{centre of the nearest edge-source cell}. So a cell
directly adjacent to non-habitat has a distance of one cell width, not zero:
the physical patch boundary lies half a cell away from either centre. Core
cells are those with distance \strong{strictly greater} than \code{depth}.

The practical consequence: with 30 m cells and \code{depth = 300}, a cell needs at
least 11 cells of habitat between it and the nearest edge cell to qualify. If
you want the distance measured to the physical boundary instead, pass
\code{depth + res/2}.
}

\section{What counts as an edge}{

\code{edge = "all"} (default) treats every non-habitat cell as an edge source,
including masked-out cells, missing cells, and everything beyond the grid.
Patches clipped by the study-area boundary lose core area accordingly, which
is conservative and usually right when the boundary is a real edge such as a
coastline.

\code{edge = "background"} treats only valid background as an edge source. Masked,
missing and off-grid cells count as habitat, so a patch running off the study
area is not penalised for it. This is the right choice when the study-area
boundary is an arbitrary analytical cut through continuous habitat.
}

\section{Geographic rasters}{

The distance transform needs uniform cell spacing, which a lon/lat grid does
not have. This function errors on lon/lat unless \code{units = "cells"}. Project to
an equal-area CRS first.
}

\examples{
\dontshow{if (diamondback_ready()) withAutoprint(\{ # examplesIf}
# A 7x7 block of habitat: with 1 m cells and depth 1, only cells more than
# one cell from the edge survive, giving a 3x3 core.
m <- matrix(0, 9, 9)
m[2:8, 2:8] <- 1
res <- patch_core_area(label_patches(m, quiet = TRUE), depth = 1, quiet = TRUE)
res$metrics[, c("patch_id", "cells", "core_cells", "core_fraction")]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=patch_metrics]{patch_metrics()}}
}
