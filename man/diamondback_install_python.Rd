% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{diamondback_install_python}
\alias{diamondback_install_python}
\title{Set up a private Python environment for diamondback}
\usage{
diamondback_install_python(prompt = interactive(), quiet = FALSE)
}
\arguments{
\item{prompt}{Ask for confirmation before downloading. Defaults to \code{TRUE} in
interactive sessions. Set \code{FALSE} for scripted or CI setup, which is an
explicit statement that the download is expected.}

\item{quiet}{Suppress progress reporting.}
}
\value{
\code{TRUE} if the environment is ready, \code{FALSE} if you declined,
invisibly.
}
\description{
The deliberate, opt-in installer. This is the \strong{only} thing in the package
that downloads software; no analysis function will ever do it for you.
}
\details{
It uses \code{reticulate::py_require()}, which resolves a \code{uv}-managed environment
containing a CPython interpreter, NumPy and SciPy. Your existing Python
installations, virtualenvs and conda environments are not touched or
modified. Consent is recorded in \code{tools::R_user_dir("diamondback", "config")}
so that later sessions use the environment without asking again.

You do not need this if you already have a Python with NumPy and SciPy —
diamondback finds it on its own.
}
\examples{
\dontrun{
diamondback_install_python()
}
}
\seealso{
\code{\link[=diamondback_check]{diamondback_check()}}, \code{\link[=diamondback_remove_python]{diamondback_remove_python()}}
}
