% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series.R
\name{track_patch_series}
\alias{track_patch_series}
\title{Track patches across a time series}
\usage{
track_patch_series(
  x,
  time = NULL,
  class = NULL,
  directions = 8,
  mask = NULL,
  na = c("outside", "background"),
  crop = NULL,
  min_overlap_cells = 1L,
  min_overlap_prop = 0,
  output_dir = NULL,
  metrics = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{x}{A multi-layer \code{SpatRaster}, a vector of raster filenames, or a list
of \code{SpatRaster}s or \link{patch_result}s.}

\item{time}{Labels for the time steps: a vector as long as \code{x}. Defaults to
layer names, file basenames, or \verb{1..n}.}

\item{class, directions, mask, na, crop}{Passed to \code{\link[=label_patches]{label_patches()}}. Note that
\code{crop} applies identically to every step, which is what keeps the series
comparable: cropping each year to its own extent would misalign them.}

\item{min_overlap_cells, min_overlap_prop}{Passed to \code{\link[=compare_patches]{compare_patches()}}.}

\item{output_dir}{Optional directory. When given, each time step is cached
there (see \code{\link[=analyze_patches]{analyze_patches()}}), so an interrupted run resumes rather than
relabelling everything.}

\item{metrics}{Also compute \code{\link[=patch_metrics]{patch_metrics()}} for each time step.}

\item{quiet}{Suppress progress reporting.}
}
\value{
A \code{patch_series} object with:
\itemize{
\item \verb{$results} --- named list of \link{patch_result}s, one per time step.
\item \verb{$transitions} --- one row per patch per transition, stacked \verb{$events}
tables with \code{time_from} and \code{time_to} columns added.
\item \verb{$overlaps} --- stacked \verb{$overlaps} tables with the same two columns.
\item \verb{$summary} --- one row per transition: patch counts, cell totals, and
event tallies.
}
}
\description{
Labels each time step, compares consecutive steps, and stacks the results
into one long lineage table. This is deliberately a chain of pairwise
comparisons: it does not try to solve persistent patch identity across the
whole series, which is a genuinely unsettled problem. What it gives you is
every consecutive transition in one tidy table, which is enough to build most
lineage analyses and is honest about what it knows.
}
\examples{
\dontshow{if (diamondback_ready()) withAutoprint(\{ # examplesIf}
# Three steps: a patch grows, then splits.
m1 <- matrix(0, 6, 6); m1[2:3, 2:3] <- 1
m2 <- matrix(0, 6, 6); m2[2:5, 2:5] <- 1
m3 <- matrix(0, 6, 6); m3[2:3, 2:5] <- 1; m3[5, 2:5] <- 1
s <- track_patch_series(list(m1, m2, m3), time = c(2000, 2010, 2020), quiet = TRUE)
s$summary
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=compare_patches]{compare_patches()}}
}
